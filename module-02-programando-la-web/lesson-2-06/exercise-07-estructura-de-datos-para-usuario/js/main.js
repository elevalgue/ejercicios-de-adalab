'use strict';

console.log('Are you ready for a good time? :))');

const userInfo = {};
const job = 'developer';

userInfo.firstName = 'Amanda'; 
userInfo.lastName = 'Palma'; 
userInfo.age = 35;
userInfo.job = job; 

console.log(userInfo, 'maricarmen');

userInfo.firstName = 'Elena';
userInfo.age = (userInfo.age) + 1;

console.log(userInfo, 'pepino');
