// 1. Debemos crear un proyecto o repo.

// 2. Debemos crear un package.json usando npm init o creándolo a mano.

// 3. Debemos instalar el módulo express ejecutando en la terminal npm install express

        // Para saber que lo hemos hecho bien, dentro del fichero package.json debe aparecer express dentro de dependencies
// 4. Debemos instalar el módulo cors ejecutando en la terminal npm install cors.
        // Para saber que lo hemos hecho bien, dentro del fichero package.json debe aparecer express dentro de dependencies
        
// 5. Debemos crear un fichero index.js dentro de la carpeta src donde programaremos el servidor.

// 6. Para arrancar debemos hacer una de las siguientes cosas:
        // Ejecutar el comando node src/index.js o...
        // Añadir al package.json el script:

                /*"scripts": {
                "start": "node src/index.js"
                }*/